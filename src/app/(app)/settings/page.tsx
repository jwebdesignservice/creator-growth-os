import { PageStub } from "@/components/page-stub";

export const metadata = { title: "Settings · Creator Growth OS" };

export default function SettingsPage() {
  return (
    <PageStub
      title="Settings"
      description="Privacy controls, notifications and account preferences."
      nextStep="Notification preferences, privacy/data controls, session management and account deletion."
    />
  );
}
