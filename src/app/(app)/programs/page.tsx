import { PageStub } from "@/components/page-stub";

export const metadata = { title: "Programs · Creator Growth OS" };

export default function ProgramsPage() {
  return (
    <PageStub
      title="Programs"
      description="Structured creator growth paths tailored to your stage, goals and category."
      nextStep="Featured program hero, program grid with status pills (In Progress / Not Started / Pro Only), Recommended For You and Learning Path roadmap — wired to the programs/lessons tables in Supabase."
    />
  );
}
