import { PageStub } from "@/components/page-stub";

export const metadata = { title: "Community · Creator Growth OS" };

export default function CommunityPage() {
  return (
    <PageStub
      title="Creator Community"
      description="Connect with creators in your category, join accountability spaces and ask questions."
      nextStep="Category spaces (Starter / Growth / Monetization / Scale), featured circle, recent discussions, upcoming events and member spotlights. Phased per brief — Phase 2."
    />
  );
}
