import { PageStub } from "@/components/page-stub";

export const metadata = { title: "Today's Missions · Creator Growth OS" };

export default function MissionsPage() {
  return (
    <PageStub
      title="Today's Missions"
      description="Your daily creator missions, assigned by category and tuned to your goals."
      nextStep="Today / This Week / All views, mission cards with type, points and due date, completion check-offs, streak summary and admin-assigned upcoming missions."
    />
  );
}
