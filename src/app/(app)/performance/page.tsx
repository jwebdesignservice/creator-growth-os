import { PageStub } from "@/components/page-stub";

export const metadata = { title: "Performance · Creator Growth OS" };

export default function PerformancePage() {
  return (
    <PageStub
      title="Performance"
      description="Manual weekly metrics for followers, reach, engagement, posts and revenue."
      nextStep="Weekly entry form, trends across the last 12 weeks, best-post reflection, lesson-learned journal, and revenue tracking for Pro users."
    />
  );
}
