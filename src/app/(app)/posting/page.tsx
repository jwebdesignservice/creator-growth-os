import { PageStub } from "@/components/page-stub";

export const metadata = { title: "Posting Plans · Creator Growth OS" };

export default function PostingPage() {
  return (
    <PageStub
      title="Posting Plans"
      description="Plan smarter. Post consistently. Grow faster."
      nextStep="Active plan with planned posts table, weekly KPI tiles (planned posts, consistency goal, best time, engagement goal), content pillars, upcoming posts, and weekly overview donut."
    />
  );
}
