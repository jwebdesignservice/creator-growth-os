import { Globe } from "lucide-react";
import { ProgramPlaceholder } from "@/components/admin/program-placeholder";

export const metadata = { title: "Sales pages · Admin" };

export default function SalesPagesPage() {
  return <ProgramPlaceholder title="Sales pages" icon={Globe} />;
}
