import { TrendingUp } from "lucide-react";
import { ProgramPlaceholder } from "@/components/admin/program-placeholder";

export const metadata = { title: "Upsell funnel · Admin" };

export default function UpsellFunnelPage() {
  return <ProgramPlaceholder title="Upsell funnel" icon={TrendingUp} />;
}
